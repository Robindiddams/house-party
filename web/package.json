{
  "name": "house-music-frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-list": "^0.8.8",
    "react-player-controls": "^0.5.18",
    "react-scripts": "1.1.1",
    "socket.io-client": "^2.0.4",
    "sockette": "^1.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "sass": "node-sass --recursive --output src/styles/css --source-map true --source-map-contents src/styles/scss",
    "sass:watch": "npm run sass -- --watch"
  },
  "devDependencies": {
    "node-sass": "^4.7.2"
  }
}
